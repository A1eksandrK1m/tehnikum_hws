import time
# Задача 1. “Калькулятор”
# Ввести числа от пользователя, сохранить их и вывести их сумму. Решить с
# помощью while, листов и sum().
# Решается с помощью цикла while
# Считывай, пока пользователь не введет 0.
# Числа и знак операции вводятся пользователем. После выполнения вычисления
# программа не должна завершаться, а должна запрашивать новые данные для
# вычислений. Завершение программы должно выполняться при вводе символа '0' в
# качестве знака операции. Если пользователь вводит неверный знак (не '0', '+', '-', '*', '/'),
# то программа должна сообщать ему об ошибке и снова запрашивать знак операции.
# Также сообщать пользователю о невозможности деления на ноль, если он ввел 0 в
# качестве делителя.



a = int(input('Введите число А '))
b = int(input('Введите число B '))
correct_symbols = ['+', '-', '/', '*', '0']
input_symbol = input('Введите символ (+, -, *, /, или введите 0 для завершения ')
x = []


while input_symbol in correct_symbols:
    if input_symbol == '+':
        print('Число', (a + b), 'добавлено в список')
        x.append(a + b)
    elif input_symbol == '-':
        print('Число', (a - b), 'добавлено в список')
        x.append(a - b)
    elif input_symbol == '*':
        print('Число', (a * b), 'добавлено в список')
        x.append(a * b)
    elif input_symbol == '/':
        if b == 0:
            print('Деление н    а ноль невозможно.')
        else:
            print('Число', (a / b ), 'добавлено в список')
            x.append(a / b)

    a = int(input('Введите число А '))
    b = int(input('Введите число B '))
    input_symbol = input('Введите символ (+, -, *, /, или введите 0 для завершения ')

    if input_symbol == '0':
        print('Сейчас подсчитаем...')
        time.sleep(3)
        print(x)  # вывожу аккумулированные числа
        print(sum(x))  # вывожу сумму ЧИСЕЛ В ЛИСТЕ
        break

