#“Ввод-вывод”
#Bвести числа от пользователя, сохранить их и вывести их сумму. Решить с
#помощью while, листов и sum()
#Решается с помощью цикла while
#Считывай, пока пользователь не введет 0.

list1 = [] #задаю пустой список, который будет исполнять роль "аккумулятора" для введенных чисел

while  True:
    a = int(input('введите первое число: '))
    b = int(input('Введите второе число: '))

    list1.append(a)  #добавляем введеный символ А в лист
    list1.append(b)  #добавляем введеный символ В в лист
    print(sum(list1)) #принтую(сумму(листа1)) / вывожу сумму элементов в списке
    if a and b == '0':
        break
